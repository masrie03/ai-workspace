# pull
>cd "path"
>git pull

# push (if ada new file)
> git status (it will show ur new file listed as untracked)
> git add . (to track all new/modified file)
> git commit -m "Add New file" (records the file in ur local git history)
> git pull origin main --rebase (to avoid push rejection)
> git push origin main

